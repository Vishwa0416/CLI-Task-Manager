# CLI-Task-Manager
A simple command-line tool to manage your daily tasks. You can add, view, mark as done, and delete tasks. Tasks are stored in a local file (tasks.json), so they persist between runs.
